spring:
  application:
    name: central-security
  datasource:
    url: "jdbc:mysql://localhost:3306/central_security"
    username: "root"
    password: "password"
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
  security:
    user:
      name: admin
      password: 123456

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
server:
  port: 0
